[pytest]
DJANGO_SETTINGS_MODULE = config.settings.development
python_files = tests.py test_*.py
# westminster_standards is measured too: it is a third of the codebase and
# was invisible in every coverage report.
addopts = --cov=catechism --cov=accounts --cov=westminster_standards --cov-report=term-missing
